# Environment & secrets (never stage — local hydration only)
.env
.env.local
.env.production
.env.development
.env.*.local
.env.development.local
.env.test.local
.env.production.local
*.pem
*.key
credentials.json
secrets.json

# GCP / OAuth (LiteLLM, Google Workspace MCP)
config/gcp-service-account.json
config/google-oauth-token.json
google-api/

# Runtime / local DB
.sandbox/
*.db
*.db-wal
*.db-shm
*.sqlite-wal
*.sqlite-shm
vault.db
data/
payload.sqlite

# Generated Hostinger deploy artifact (push from vader-site-deploy/ to its own GitHub repo)
vader-site-deploy/

# Vader Construct dashboard (ui/dashboard — Lean Boundary)
ui/dashboard/node_modules/
ui/dashboard/.next/
ui/dashboard/.env.local

# Consumer app artifacts (when copied into Next/Payload project)
node_modules/
.next/
dist/
build/
out/

# Logs
logs/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Playwright E2E artifacts
test-results/
playwright-report/
playwright/.cache/

# Local-only operator docs (not published to GitHub)
.cursor/docs/BOILERPLATE-IMPROVEMENT/
.cursor/docs/README/

# Local-only design captures (committed brand media → media/ or docs/assets/README.md)
.cursor/design_references/*.jpg
.cursor/design_references/*.jpeg
.cursor/design_references/*.png
.cursor/design_references/*.webp
.cursor/design_references/*.gif
.cursor/design_references/vader-ops-dashboard.zip

# MCP / agent screenshots (local verification only — not repo media)
assets/

# Environment & secrets (never stage — local hydration only)
.env
.env.local
.env.production
.env.development
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Personal secrets vault (generated — never commit)
.cursor/env/Personal-Secrets-Vault.md
.cursor/ENV-Files/
*.pem
*.key
credentials.json
secrets.json

# GCP / OAuth (LiteLLM, Google Workspace MCP)
config/gcp-service-account.json
config/google-oauth-token.json
google-api/

# Runtime / local DB
.sandbox/
*.db
*.db-wal
*.db-shm
*.sqlite-wal
*.sqlite-shm
vault.db
data/
payload.sqlite

# Generated Hostinger deploy artifact (push from vader-site-deploy/ to its own GitHub repo)
vader-site-deploy/

# Vader Construct dashboard (ui/dashboard — Lean Boundary)
ui/dashboard/node_modules/
ui/dashboard/.next/
ui/dashboard/.env.local

# Consumer app artifacts (when copied into Next/Payload project)
node_modules/
.next/
dist/
build/
out/

# Logs
logs/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Playwright E2E artifacts
test-results/
playwright-report/
playwright/.cache/

# Local-only operator docs (not published to GitHub)
.cursor/docs/README/
# Legacy upgrade runbooks → _archive/boilerplate-improvement/ (optional local copies)

# Local-only design captures (v0 mocks — see media/design-references/README.md)
media/design-references/**/*.jpg
media/design-references/**/*.jpeg
media/design-references/**/*.png
media/design-references/**/*.webp
media/design-references/**/*.gif
media/design-references/**/*.pdf
media/design-references/**/*.zip

# MCP / agent screenshots (local verification only — not repo media)
assets/

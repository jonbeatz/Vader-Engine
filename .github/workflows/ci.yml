name: CI
on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Hydrate Core Dependencies
        run: npm ci

      - name: Run Global Credentials Scan Gate
        run: npm run msc:validate-env

      - name: Verify MCP Registry
        run: npm run verify:mcp

      - name: Run Biome Lint
        run: npm run msc:lint

      - name: Run Project Integration Self-Grader
        run: npm run grade

      - name: Run Root Structural Tests
        run: npm run msc:test:root

      - name: Validate Next.js Minimal Workspace Sandbox
        run: |
          cd examples/nextjs-minimal
          npm ci
          npm run test

      - name: Validate Payload Full-Stack Sandbox
        env:
          PAYLOAD_SECRET: ci-build-placeholder-secret-min-32-chars-long
        run: |
          cd examples/nextjs-payload
          npm ci
          npm run build

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium firefox

      - name: Run Multi-Sandbox E2E
        env:
          PAYLOAD_SECRET: ci-build-placeholder-secret-min-32-chars-long
        run: |
          mkdir -p examples/nextjs-payload/database
          rm -f examples/nextjs-payload/database/payload-e2e.db*
          npm run msc:e2e

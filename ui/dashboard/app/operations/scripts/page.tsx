import { MscPlaceholderPage } from '@/lib/placeholder-page';

export default function OperationsScriptsPage() {
  return (
    <MscPlaceholderPage
      title="Scripts"
      description="Allowlisted npm scripts dispatched via POST /api/run-script from the command palette."
    />
  );
}

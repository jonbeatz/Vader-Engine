import { MscPlaceholderPage } from '@/lib/placeholder-page';

export default function OperationsEnvPage() {
  return (
    <MscPlaceholderPage
      title="Environment"
      description="Hydration from .env.local → .env.example — keys only in committed contract."
    />
  );
}

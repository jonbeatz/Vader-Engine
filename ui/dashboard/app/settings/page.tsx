import { MscPlaceholderPage } from '@/lib/placeholder-page';

export default function SettingsPage() {
  return (
    <MscPlaceholderPage
      title="Settings"
      description="Dashboard host, port, and optional API token configuration (MSC_DASHBOARD_*)."
    />
  );
}

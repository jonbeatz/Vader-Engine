import { MscPlaceholderPage } from '@/lib/placeholder-page';

export default function TemplatesPage() {
  return (
    <MscPlaceholderPage
      title="Templates"
      description="Read-only blueprints under templates/ — list, apply, and seed via msc:template CLI."
    />
  );
}

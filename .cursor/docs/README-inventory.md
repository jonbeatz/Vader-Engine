# README Inventory (auto-generated)

> Generated by `npm run inventory` (`scripts/msc-generate-inventory.mjs`). Do not hand-edit tables — re-run the script.

**Last run:** 2026-05-21T17:07:00.640Z  
**Project:** msc-universal-boilerplate @ 1.0.0

---

## MCP servers (`.cursor/mcp.json`)

| Server | Transport | Target | Health (.env.local) |
|--------|-----------|--------|---------------------|
| payload | stdio | -y @govcraft/payload-cms-mcp@latest | — (no key required) |
| github | stdio | -y @modelcontextprotocol/server-github@latest | OK |
| playwright | stdio | -y @playwright/mcp@latest --browser chrome --allowed-hosts 127.0.0.1,localhost --caps devtools | — (no key required) |
| filesystem | stdio | -y @modelcontextprotocol/server-filesystem@latest ${workspaceFolder} | — (no key required) |
| fetch | stdio | -y mcp-server-fetch@latest | — (no key required) |
| tavily | stdio | -y tavily-mcp@latest | OK |
| firecrawl | stdio | -y firecrawl-mcp | Missing (.env.local) |
| resend | stdio | -y resend-mcp | Missing (.env.local) |
| sequential-thinking | stdio | -y @zengwenliang/mcp-server-sequential-thinking | — (no key required) |
| wordpress-local | stdio | -y @automattic/mcp-wordpress-remote@latest | Missing (.env.local) |
| neon-postgres | streamableHttp | https://mcp.neon.tech/mcp | — (no key required) |
| shadcn | stdio | shadcn@latest mcp | — (no key required) |
| context7 | stdio | -y @upstash/context7-mcp@latest | — (no key required) |

---

## npm dependencies (`package.json`)

| Package | Version |
|---------|---------|
| dotenv | ^16.4.7 |
| husky | ^9.1.7 |

---

## Regenerate

```bash
npm run inventory
```

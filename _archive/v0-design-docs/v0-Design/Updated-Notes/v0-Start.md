I am attaching my `v0-Vader-Engine-Prompt.md` and 6 visual reference images.

Please read the prompt as the authoritative technical specification for the **Vader Protocol v2.5.0-Engine** dashboard. Use the attached images as your visual source of truth for layout, spacing, and branding.

Your primary goal is to generate a high-fidelity, functional Next.js 15 prototype that perfectly mirrors the Bento grid architecture, footer signature, and command palette logic defined in the specification.

**Begin by generating the core `layout.tsx` shell and the main Dashboard view**, ensuring:
- The footer `Powered by MSC Media Engine` is present on every page
- The command bar `Type / for commands` is at bottom-left and opens the Command Palette
- Dark surfaces: page `#121212`, cards `#1c1c1c`
- All interactive elements include required `data-testid` attributes

**Generate the complete dashboard now.**

